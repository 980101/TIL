### 오늘 배운 것
#### 제네릭(Generics)
: 클래스, 인터페이스, 메서드에서 사용할 데이터 타입을 일반화하여 작성하는 방법
타입을 컴파일 시에 미리 체크할 수 있어 안전하고 유연한 코드 작성이 가능합니다.
즉, 다양한 타입을 처리할 수 있는 재사용 가능한 코드 작성이 목적입니다.

✅ 기본 문법
```java
// 제네릭 클래스 선언

public class Box<T> {
    private T item;

    public void setItem(T item) {
        this.item = item;
    }

    public T getItem() {
        return item;
    }
}

Box<String> stringBox = new Box<>();
stringBox.setItem("Hello");
String str = stringBox.getItem();  // 캐스팅 불필요
```
* < T > : 타입 파라미터 (Type Parameter)
* 제네릭 타입을 지정할 때는 <String>, <Integer>처럼 구체적인 타입을 지정
  
📌 왜 쓰냐면?
</br>예전에는 컬렉션에서 모든 객체를 Object 타입으로 저장했기 때문에,
</br>데이터를 꺼낼 때마다 형변환이 필요했고 이 과정에서 오류가 발생
</br>👉 제네릭 도입 후,자주 타입 캐스팅 할 필요가 없어서 타입 안정성과 가독성이 크게 향상

