### 오늘 한 일
* 생활코딩 JAVA 입문 3~5강 듣기
  * 데이터 타입에 따라 연산(처리)방식이 다르다.
  * 숫자 데이터는 단순 숫자로, 문자열 데이터는 ""(큰따옴표)로 감싸진 형태로 표현한다.
  * 문자열 데이터는 +를 결합연산자로 사용한다.
  * Math : 클래스. 기본적인 숫자연산을 수행하는 메소드를 포함한다. / <Math.>를 타이핑하면 숫자 데이터에서 사용할 수 있는 연산자가 나온다. ex) Math.floor(3.14)
